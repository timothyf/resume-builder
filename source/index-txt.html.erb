
<% partial(:data, :locals => { :resume_data => nil }) %>


<% @layout_data.content.right.each do |section| %>
  <%= partial "templates/#{section.template}", locals: {display_name: section.display_name} %>
<% end %>

<% @layout_data.content.center.each do |section| %>
  <%= partial "templates/#{section.template}", locals: {brief: false, display_name: section.display_name} %>
<% end %>
